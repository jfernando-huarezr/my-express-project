<h1><%= title %></h1>

<% if (genre_list.length) { %>
<ul>
  <% genre_list.forEach(genre => { %>
  <li>
    <a href="<%= encodeURI(genre.url) %>"><%= genre.name %></a>
  </li>
  <% }) %>
</ul>

<% } else { %>
<p>There are no genres</p>
<% } %>
