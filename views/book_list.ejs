<h1><%= title %></h1>
<% if (book_list.length) { %>
<ul>
  <% book_list.forEach(book => { console.log(book)%>
  <li>
    <a href="<%= encodeURI(book.url) %>"> <%= book.title %> </a> ( <%=
    book.author.name %> )
  </li>
  <% }) %>
</ul>
<% } %>
